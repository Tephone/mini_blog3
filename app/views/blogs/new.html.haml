- if @blog.errors.any?
  - @blog.errors.full_messages.each do |msg|
    .ul
      .li= msg
%h3 miniブログ作成画面
= form_with(model: @blog) do |f|
  = f.label :content
  = f.text_field :content
  = f.submit  
